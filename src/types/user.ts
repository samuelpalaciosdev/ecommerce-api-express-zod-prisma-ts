export type User = {
  id: string;
  name: string;
  lastName: string;
  email: string;
  password: string;
  isActive: boolean;
  role: string;
};

export type tokenUser = {
  id: string;
  name: string;
  lastName: string;
  email: string;
  isActive: boolean;
  role: string;
};
